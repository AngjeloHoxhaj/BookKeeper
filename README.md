# BookKeeper
The program is a console-based book management system in C, using a doubly linked list to store and manage records. It loads book data from a file at startup. Users can add, delete, or edit books by ISBN, sort by fields like title or year, search/display specific books, or show all records. Data is saved on exit.
